## [Unreleased]

### 1.3.2 — 2026-09-25（listing 二修：按 M121 第一读者对照,文案逐句退回正文有依据的宣称）

- 首轮重写复述过关但 8/8 存在无依据或夸大机器承诺处,本条为其修正:机器面话术与正文/门禁脚本逐句对齐,正文没有的（如固定判断位、逐数必拦、固定七维、TTM）一律从对外字段删除或改写为正文实际机制。

### 1.3.1 — 2026-09-25（listing 文案重写为用户视角，C-36 三闸）

- summary 收紧为一句话；description 改「拿到什么 / 机器挡什么 / 适合与不适合」三段式；契约与流程细节退回正文与包内文档，对外字段不再当合规容器。功能、门禁与数据语义相对上一版零变化。

# 更新日志

本文件自 1.3.0 起维护；此前的版本变更未在本仓库留档（该 skill 于 `21168d7` 一次性并入本仓时为 v1.2.0）。

## [1.3.0] — 2026-09-18

### Fixed（实测发现并修复）

- **`overlap_check.py` 分组退化导致静默漏检**（危害最高）：
  分组正则 `GROUP_RE` 仅匹配 `部_篇_编号` 命名（如 `01_phil_018.md`）；不匹配时旧版退化为
  **每个文件自成一组**，组内仅 1 片 → 相邻比较一次都没发生，却打印「✅ 未发现重叠」。
  而 RUNBOOK 推荐的命名正是 `001.md` / `001.txt`，**照官方指引命名即 100% 失效**。
  - 修复：不匹配时退化为统一组做全序列比较（而非各自成组）；
  - 无文件匹配命名模式时打印提醒；
  - 组内仅 1 片时显式标注「无相邻片可比较」；
  - **实际比较次数为 0 且片段数 > 1 时改为告警并 exit 3**，不再假绿。
  - 实测：修复前 `001/002/003`（S.1–2 / S.2–3）报「未发现重叠」；修复后正确报出 `001 ⟷ 002` 重叠 1 页（exit 2）。具名模式分组与索引逆序跳过均无回归。
- **`build_reader.py` 缺 `markdown` 时抛 traceback**：与 SKILL.md「不抛 traceback、给可执行下一步」的承诺不符（同类场景下 `merge_build.py` 是优雅降级）。改为友好报错 + 安装命令 + 说明为何此处无法降级。
  - 顺带修掉自相矛盾的提示：预检先说「未安装仍可运行」，紧接着却因缺 `markdown` 退出。`check_python_deps()` 新增 `hard=` 参数，对必装依赖明确提示「无法降级运行」。
  - 实测：`merge_build.py` 仍降级继续生成主稿（exit 0），无回归。

### Added

- `references/` 规范目录（对齐 journal-draft 结构）：`pitfalls.md`、`qa-checklist.md`、`reader-build.md`、`parallel-translation.md`。
- SKILL.md 新增 §十「参考文件」指针表；硬规则编号化（可交叉引用，如「硬规则 8」）。
- `CHANGELOG.md`（本文件）与 `.gitignore`。

### Changed

- SKILL.md 中五大陷阱、并行策略、增强阅读版三节收敛为「决策要点 + 指针」，全文移入 `references/`（内容不丢失）。
- 质检章节补脚本阈值指针（`MINLEN=15` 等），避免「全绿 = 没查」的误判。
- `manifest.yaml` version 同步 1.3.0，triggers 补英文触发词（对齐本仓 i18n 约定）。

### 已知未改（有意保留）

- `dedup_boundary.py` 的 `MINLEN=15`：短句误判率高，保留阈值，改以文档说明——短句边界重复需人工核对。
