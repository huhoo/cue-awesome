# catalyst-calendar

**[中文](README.md) · [English](README.en.md)**

**把你持仓里「确定的日子」排成一张日期正排的日历——每个日期背后都是一条查得着的公告或一条法条，仅此而已，不加判断。**

它回答的真实问题很朴素：**「未来三个月，我这些持仓各自有什么日子要盯？」**
回购方案期限哪天截止（公告明写的才算）、激励归属是哪天、股东会与说明会何时开、分红除权什么时候、
问询回复截止在哪、ST 生效/复牌是哪天、年报还有多少天法定期限——
这些事散在公告流里，人肉盯会漏，交给日历：`buyback`/`esop`/`disclosure_cn`/`regulatory_cn`/`statute`
五域直查为主，深研最多 1 次补解禁/减持原表，产出 `calendar.md` + 可回查来源表。

**输入**：主体集（官方全称或代码，≤10 个）+ 前瞻窗口（缺省今起 90 天）
**输出**：`calendar-<日期>-<n>主体/` —— `calendar.md`（日期正排表+「未来 30 天要盯」段+来源附录）· `sources.jsonl` · `progress.md` 台账
**代价**：四段管线+一道门禁；research ≤1 次（可零），花之前必问

---

## 一、是什么 / 不是什么

| 你要 | 用什么 |
|---|---|
| 持仓事件日历（带披露锚的日程表） | **本 skill** |
| 某只票的财报深度点评 | 姊妹件 `cn-earnings-note` |
| 选股、择时、「这个日子是不是利好」 | 不做——**事件只陈述，利好利空判词全禁**（日历上「问询回复截止」旁边的永远只是日期和锚） |
| 传闻/预期里的「将回购」「拟减持」 | 不做——未披露不入表 |

## 二、快速开始（三步）

1. **装**：本目录放进 agent 的 skills 目录。
2. **说**：「我的持仓是美的集团、东方雨虹、宁德时代，给我排未来 90 天的催化剂日历」（中英触发词都行）。
   ≤4 问补齐输入契约（主体集/窗口/关注类/输出目录，逐条带默认值），开工建台账。
3. **收**：`+scope → +events → +derive → +build` 后过门禁 `+check`（`check_calendar.py` 机检 v4，
   §v2-B+§v3-B+§v4-A/B 全量；正式调用参数以 `SKILL.md` §3 命令式为准），**不过不交付**。

## 三、防的就是日期幻觉

这件东西最大的风险不是分析错，是**编出一个不存在的日期**。硬约束按分类学 v2 的「入类第一闸」执行：

- 每条先过二选一：已有披露在场**且含明确日期**，或法定推导**且适用条件在法条锚内可判**——过不了闸根本不进表；
- **不做任何日期算术**：公告没写完整截止日，「公告日+360 天」这类推算一律不产行（v1 曾越权授权，评审后全线撤权）；
- 每条日期回指公告索引号 / 函件号 / URL / `conv_id`+路径，列级校验，别的列拿数字冒充锚即 FAIL；
- 窗外条目走六列形制的独立第 5 列「窗外余档」（锚列永远末列纯锚，注锚不互挤）；空置即 FAIL；查了没有就写「未检索到」——**空日历合法，编造日历违法**。

回购 1%/2% 进度这类无事前日期的「已发生动态」放日历末附注区，不冒充前瞻事件。

## 四、首次开通 Cue（三步，可跳过）

注册 <https://cuecue.cn>（新账号注册送 500、每日 10，以服务端为准）→ <https://cuecue.cn/hub/api-key> 取 `CUE_API_KEY` 入本地凭据（别贴进聊天）→ 装 `cue-data-mcp` / `cue-research` / `cue-omni-reader`。
**不装不报错**：降级为用户贴公告清单（标直供）；research 不发起则解禁/减持类整类「未检索到」，绝不以常识补日。所有消耗花之前先问。

## 五、包里有什么

```
SKILL.md                        主指令：铁律/输入契约/四段管线/输出契约/降级/边界（必读）
README.md / README.en.md        本文件（中文权威/英文译文）
CHANGELOG.md                    版本记录
references/event-taxonomy.md    六类事件分类学 + 拆行/余档/L3 判例
scripts/check_calendar.py       机检 v4（§v2-B+§v3-B+§v4-A/B 全量，纯 stdlib，--help 可跑）
```

## 六、当前状态（诚实清单）

- **对抗审已闭（四轮：阻断项由 6 条逐轮收到 1 条后通过，含真实冒烟反哺的三道闸），已随仓公开**；**满窗多主体端到端交付跑已回填（补跑于 2026-09-21）**：10 主体×90 天窗×evidence 全链强制，check_calendar v4 全式 exit 0；满窗实况：10 主体×90 天窗内，可锚前瞻条目 1 行（临时股东会，原文载明会期），其余 9 主体为空前瞻+归因句；三季报法定期限 2026-10-31 规范上可推、当场 statute 取回无原文锚故不落行（拒推不造锚）——首跑漏执行 `+derive` 属管线缺位，已补跑并制度化申报。
- **未提交技能市场**：上架（P2）整体冻结中，与姊妹件同闸，时机由 Owner 定；仓库内可见性以根 README 与 `CHANGELOG.md` 为准。
- 主体集上限 10 个（机检计数）；`margin`（两融折算率）有意不入列——与事件日历无关。
- 日历不含行情数值流（`equity_market` 通道未开放）；**评级词汇零容忍是机检事实**——评级在本件根本不适用，没有留白位可填。

## 七、出处与致谢

- 基座：anthropics/financial-services `catalyst-calendar`（Apache-2.0）——日历形态与任务结构的来源。
- A股事件分类学与法定期限推导为本包按披露制度**重实现**，非搬运；数据层完全 Cue 通道化，无基座代码。详见仓库根 `NOTICE.md`。
