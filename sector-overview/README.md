# sector-overview

**[中文](README.md) · [English](README.en.md)**

「这行业回暖了。」——凭什么？

大多数行业报告答不上这个问题：判断词满天飞，翻遍全文找不到一条可比序列。本件反着做：
**输入行业名 + 窗口，还你一份六节简报（≤6 页），其中每一个「升/降/回暖/承压/拐点」都必须同句挂着
它引用的那条序列（S 锚，含口径与截止日期）——挂不上的句子，就老实写「无可比序列，不做景气判定」。**
薄一点没关系，没锚的判断一句不给。

**输入**：行业（申万/中信一级或口语名，歧义列候选拒猜）+ 窗口（缺省近 4 个完整季度）+ 可选角度
**输出**：`sector-<行业>-<窗口>/` —— `report.md`（画像与结论/量价与驱动/供需与产能/格局与代表公司/政策时间线/复核清单）· `sources.jsonl` · 台账
**代价**：五段管线+四道机检；深研 ≤1 次（发起前报成本征询）；默认不碰任何预期类均值

---

## 一、和另几件怎么分

| 你要 | 用什么 |
|---|---|
| 一个行业近四季的冷热证据（每个判断词当场可查凭据） | **本 skill** |
| 某家公司财报深度点评 | `cn-earnings-note`（覆盖账本/beat-miss 在其契约内） |
| 见客户前 30 秒的一页纸 | `tear-sheet` |
| 持仓未来三个月的日期表 | `catalyst-calendar` |
| 「看好哪个行业」 | 哪儿都不给——本件评级词零容忍且**无豁免区** |

## 二、为什么值得用（三条硬话）

- **判断词-锚同行**是本件的命门，也是机检第二道的原文：`升|降|回暖|承压|走弱|拐点|高景气|低谷`
  出现在正文 ⇒ 同句含 `S<n>` 或走逐字态二句——「拐点」类词无锚，按本套件的定义就是幻觉（catalyst 日期幻觉的行业版）；
- **维度不背模板**：每次运行先由深研按行业类型动态生成 2–4 个分析维度（养殖看存栏、光伏看排产），
  不套上一个行业用过的框架库——行业是每次现问的，不是每次默写的；
- **协会口径全标 L3**、进复核清单；行业均值的预期类数字默认不取（高危超售话术，从源头不碰）。

## 三、快速开始（三步）

1. **装**：本目录放进 agent 的 skills 目录。
2. **说**：「光伏行业近况盘一下，重点看排产和价格」（触发词中英都行）。≤3 问补齐输入契约。
3. **收**：五段管线（`+scope → +series → +research → +policy → +companies`）后过 `check_sector.py` 四道，**不过不出**。
   机检 `check_sector.py` 已落地（守护样全绿，数量以 CHANGELOG 最近验收行为准）；对抗审按新制度闭环（独立评审方出题、判卷通过），真实冒烟的形状教训已回哺入守护样。

## 四、首次开通 Cue（三步，可跳过）

注册 <https://cuecue.cn>（新账号注册送 500、每日 10，以服务端为准）→ <https://cuecue.cn/hub/api-key> 取 `CUE_API_KEY` 入本地凭据（别贴进聊天）→ 装 `cue-data-mcp` / `cue-research`（`cue-omni-reader` 按需）。
不装不报错：你贴协会月报/统计原文也能跑（标「用户直供」），但判断词-锚纪律不放松——直供素材当锚挂得上的才配判断词。

## 五、包里有什么

```
SKILL.md                         主指令：命门纪律/输入契约/五段管线/输出契约/套件连携（必读）
README.md / README.en.md         本文件对
CHANGELOG.md                     版本记录
references/section-skeleton.md   六节骨架+每节 [执行蓝图]
references/policy-timeline.md    政策时间线行规格（statute 三件齐锚口径引 catalyst，不复制）
scripts/check_sector.py          四道机检（已落地；守护样全绿，数量以 CHANGELOG 最近验收行为准）
```

## 六、当前状态（诚实清单）

- **建材量价半程实跑已回填（2026-09-21）**：research 1 次（约 13 分钟）+直查 7 次+omni 0；画像/量价/供需三节由全态二升到逐行带源（深研二手快照一律标 L3、挂人工复核）。**全仓唯一权威计数，口径写死：量价与驱动节 6 行——每行同时带「口径」列与来源锚；这个数只是量价节的表行数，不是全报告带源行数，也不等于 L3 层行数。** 同价双源（沥青两个报价）并列不仲裁=契约设计；连续周频序列仍不可得（五项「未检索到」在案）：**半程锚通≠量价序列能力可宣**，维度生成质量维持设计值口径。
- **审结出门**：对抗审通过与真实冒烟反哺均已闭，随仓公开（版本现值以本包 `CHANGELOG.md` 与 frontmatter 为准）；端到端满量交付跑未回填（半程与冒烟均≠满量交付），跑通前按设计值对待；域扩容候工程（两条内部跟踪项：一条已半缓解、一条新账在途）。
- **未提交技能市场**：P2 整体冻结中，时机由 Owner 定；仓库内可见性以根 README 与 `CHANGELOG.md` 为准。
- 图不做（表带 asof）；不含行情数值流与预期均值（`equity_market` 未开放+高危话术自缚）；个股只做聚合不做点评。

## 七、出处与致谢

- 基座：anthropics/financial-services `sector-overview`（Apache-2.0）——六节型任务结构的来源。
- 本包为**A股化重实现而非搬运**：判断词-锚同行、维度动态生成、政策锚规格均为本套件自建纪律，无基座代码。出处声明见仓库根 `NOTICE.md`。
